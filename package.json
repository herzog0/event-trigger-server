{
  "name": "event-trigger-server",
  "author": "Teodoro B. Mendes",
  "description": "Listen and deliver your database events in realtime to any endpoint",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "install:build": "npm install && npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.7.0",
    "@types/node-fetch": "^2.6.6",
    "@types/pg": "^8.10.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
